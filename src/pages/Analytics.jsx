import AnalyticsIndex from "../components/analytics";

function Analytics({user}) {
    return (
        <div>
            <AnalyticsIndex user={user} />
        </div>
    );
}

export default Analytics